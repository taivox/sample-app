# frontend

## Verify
`npx eslint src/`

## Structure
- Vite + React 19
- Runtime config via `public/config.js` (window.REACT_APP_API_URL)
- JSX files require `.jsx` extension

## Note
react-scripts removed - now uses Vite
